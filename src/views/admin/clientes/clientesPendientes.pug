doctype html
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Vive natural 
        link(rel="stylesheet", href="/css/main/dashboard.css")
    body 
        // menu de administrador
        include ../../partials/menuAdmin.pug
        main 
            article 
                h1 Clientes Pendientes
                p.texto-explicativo
                    | Aquí puedes ver los clientes pendientes de aprobación y también crear usuarios temporales para registrar compras de clientes sin cuenta.
                div.admin-clientes-container
                .tabs
                    button.tab-btn.active(data-tab="pendiente") Pendientes
                    button.tab-btn(data-tab="aprobado") Aprobados
                    button.tab-btn(data-tab="rechazado") Rechazados/Bloqueados

                .tab-content.active#clientes-pendiente-container
                .tab-content#clientes-aprobado-container
                .tab-content#clientes-rechazado-container

        //- Script en Pug
        

        //- CSS en Pug
        style.
            .admin-clientes-container {
                max-width: 1200px;
                margin: 0 auto;
            }

            .tabs {
                display: flex;
                margin-bottom: 20px;
                border-bottom: 1px solid #ddd;
            }

            .tab-btn {
                padding: 10px 20px;
                background: none;
                border: none;
                cursor: pointer;
                font-size: 16px;
                border-bottom: 3px solid transparent;
            }

            .tab-btn.active {
                border-bottom-color: #5A60FF;
                font-weight: bold;
            }

            .tab-content {
                display: none;
            }

            .tab-content.active {
                display: block;
            }

            .cliente {
                margin-bottom: 15px;
                transition: all 0.3s ease;
            }

            .cliente-aprobado {
                border-left: 4px solid #28a745;
            }

            .cliente-rechazado, .cliente-bloqueado {
                border-left: 4px solid #dc3545;
            }

            .cliente-pendiente {
                border-left: 4px solid #ffc107;
            }


        script(type="module", src="/js/pages/admin/clientesAdminPages.js") 
                